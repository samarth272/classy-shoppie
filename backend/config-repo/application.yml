spring:

  kafka:
    bootstrap-servers: classy-kafka:9092

  data:
    redis:
      host: classy-redis
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus